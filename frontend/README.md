# Frontend

React + Vite UI for the approval workflow.

## Run

```bash
cd frontend
npm install
npm run dev
```

The app talks to the Django backend at `http://127.0.0.1:8000`.

In development, Vite proxies `/api` requests to the backend, so make sure the Django server is running before signing in.
